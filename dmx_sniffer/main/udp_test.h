/**
 * @file udp_test.h
 * @brief UDP-сервер для стресс-тестирования DMX-приёмников
 *
 * Предоставляет UDP-эндпоинт для быстрого получения данных обоих DMX-портов.
 * Используется совместно с C++ тестом (stress_test.cpp) на PC для проверки
 * синхронизации P0/P1 при высоком FPS (30fps, 15 минут).
 *
 * Протокол:
 *   PC -> ESP: 1 байт команды (0x01 = get blob)
 *   ESP -> PC: 1024 байта (P0[512] + P1[512])
 *
 * Преимущество над HTTP: UDP-запрос ~0.1мс vs HTTP ~60мс.
 * Позволяет достичь реальных 30fps в тесте.
 */

#pragma once

/**
 * @brief Инициализировать UDP-сервер для стресс-теста
 *
 * Создаёт задачу FreeRTOS на ядре 1 (приоритет 5).
 * Задача слушает UDP-порт 5124 и отвечает на запросы команды 0x01.
 * Вызывается один раз из app_main().
 */
void udp_test_init(void);
